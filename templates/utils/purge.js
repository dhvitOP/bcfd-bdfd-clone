const { Message, Client } = require("discord.js");

module.exports = {
    name: "purge",
    aliases: ['clear'],
    
    /**
     *
     * @param {Client} client
     * @param {Message} message
     * @param {String[]} args
     */
  execute: async(client, message, args) => {
      
    }
}